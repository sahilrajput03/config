# ~ Sahil: LEARN: Below instructions need to be added to /etc/sudoers file.
# ~ Sahil: You can either use `sudoCode /etc/sudoers` or `sudo visudo` to edit that file and add below code to that file. 

# ~ Sahil ~
# LEARN: Default value is 5 (in mins)
# LEARN: Value can't be a fraciont i.e, 3.5
# LEARN: Setting a value less than 0 means to never ask again ever
Defaults:array timestamp_timeout=-1
## ~ Sahi ~ Don't require password for each new terminal
Defaults:array timestamp_type=global[array@manjarolinux ~]$
