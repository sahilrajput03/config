{
    "events": {
        "start": "clear; tmux clear-history 2> /dev/null",
	"comment": "Above command is tmux safe, i.e., if tmux is not there then it won't throw error, YIKES!",
        "comment:start": "clear"
    }
}
